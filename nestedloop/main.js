// Multiplication of number two number
for (let firstNum = 0; firstNum < 2; firstNum++) {
    for (let secondNum = 0; secondNum <= 9; secondNum++) {
        console.log(firstNum,"times", secondNum, "is equals to", firstNum * secondNum)
    }
}